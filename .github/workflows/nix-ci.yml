name: Nix CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  nix-ci:
    uses: hermetic-foundation/.github/.github/workflows/nix-ci.yml@main
    with:
      publish_cache: true
    secrets: inherit
